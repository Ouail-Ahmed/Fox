        # file_path = Path(indexer.path)  # Path
        # for keyword, weight in indexer:
        #     p = Posting(file_path.stem , weight , str(file_path))
        #     ind = Index( keyword,int(file_path.stem) , p)
        #     inverse_index_access.insert_index(ind)